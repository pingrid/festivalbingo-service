# festivalbingo-service

This is the festival bingo backend service.

```
users (id, username, email, game)
games (id, name)
boards (id, ownerid, gameid, dimension)
tiles (id, boardid, xpos, ypos)
fields (id, tileid, rowid, ar   tist)
```